{
  "name": "zxcoder",
  "private": true,
  "packageManager": "npm@11.2.0",
  "scripts": {
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "build": "turbo run build"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
