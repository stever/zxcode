type Mutation {
  compile(
    basic: String!
  ): CompileResult
}

type Mutation {
  compileC(
    code: String!
  ): CompileResult
}

type Mutation {
  compileSjasmplus(
    code: String!
  ): CompileResult
}

type Mutation {
  compilePascal(
    code: String!
    machine: String
  ): CompileResult
}

type CompileResult {
  base64_encoded: String!
}

