{
  "name": "@zxplay/emulator",
  "version": "2.0.0",
  "private": true,
  "description": "Shared ZX Spectrum emulator engine (zx_go wasm core) for ZX Play apps",
  "main": "index.js",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "build": "npm --prefix ../emulator-core run build && mkdir -p dist && cp ../emulator-core/dist/zx.wasm ../emulator-core/dist/wasm_exec.js dist/ && cp src/zxgo/zx-feeder.worklet.js dist/",
    "build:debug": "npm run build",
    "clean": "del-cli dist"
  },
  "dependencies": {
    "events": "^3.3.0",
    "file-dialog": "^0.0.8",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "del-cli": "^7.0.0"
  }
}
