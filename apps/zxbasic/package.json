{
  "name": "zxbasic",
  "private": true,
  "description": "ZX Play compile API for Boriel ZX Basic (Python/FastAPI). Built and run via Docker; this manifest just registers it as a workspace and wraps the dev/test commands.",
  "scripts": {
    "dev": "uvicorn app.main:app --reload",
    "test": "pytest"
  }
}
