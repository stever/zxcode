*.DS_Store
*.interp
*.swp
*.tokens
/.graphqlconfig
/.idea/
/build/
/es5/
/node_modules/
/public/dist/*
# Build artifacts emitted by webpack/HtmlWebpackPlugin + the manifest step.
/public/index.html
/public/asset-manifest.json
# webpack-dev-server HMR chunks flushed to disk during `npm run dev`.
/public/*.hot-update.*
# Emulator runtime assets are copied in from @zxplay/emulator at build time
# (see copy:emulator); the package is their source of truth, not this app.
/public/roms/
/public/tapeloaders/
/schema.graphql
/typedocs/
